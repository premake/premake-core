Specifies a list of files to treat as build resources in XCode.

```lua
xcodebuildresources { "paths" }
```

### Parameters ### 

`paths` specifies a list of paths to treat as build resources.

### Applies To ###

Project configurations.

### Availability ###

Premake 5.0.0-alpha12 or later for XCode.
